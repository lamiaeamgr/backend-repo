PORT=5000
# MongoDB reachable from the backend container (Docker Desktop / Linux with host gateway).
# Start Mongo on the host on 27017, or point to your server.
MONGODB_URI=mongodb://host.docker.internal:27017/backend_repo_db
