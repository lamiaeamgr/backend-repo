PORT=5000
# For docker compose: hostname is the service name in docker-compose.yml (mongo).
# For manual "docker run" with --name backend-mongo, use mongodb://backend-mongo:27017/backend_repo_db
MONGODB_URI=mongodb://mongo:27017/backend_repo_db
